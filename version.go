package main

// appName and version are the single source of truth for the app's identity,
// shown in the tray tooltip and the About dialog. Bump version here on release.
const (
	appName = "Deltatronix agent"
	version = "0.1.0"
)
