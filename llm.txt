# Deltatronix agent — local LLM backend (OpenAI-compatible API).
# Managed by the Settings window; hand-edits are overwritten when you save there.
# Headless users may edit these keys directly, then restart: dtx-agent run

# Backend URL — Ollama :11434/v1, LM Studio :1234/v1, or any OpenAI-compatible server.
backend_url=http://localhost:11434/v1

# ── Logging ─────────────────────────────────────────────────────────────────
# log:              on (default) | off
# log_dir:          folder for dtx-agent.log (default: the config folder)
# log_max_size_mb:  rotate each file at this size
# log_max_files:    how many rotated files to keep before overwriting
log=on
log_dir=
log_max_size_mb=10
log_max_files=3
